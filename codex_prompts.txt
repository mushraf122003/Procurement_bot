I am creating a procurement bot using langchain, langraph, google llm model, Hugingface data chunking and embedding models. Create an folder in this project folder and name it as code. inside the code folder I want you to write the code first for data ingestion pipeline. where I will puling the PDF files and Excel files from my folder called data which present in the project folder itself. then I need to perform chunking and embedding for the files using langchain document loaders. then I need this data to be transformed and stored in Mongo DB Atlas in and create an Vector search index to perform Vector search. 

Then create the retravel pipeline where we get the user query and perform embedding on that query and use that to retrieve the context from the Mongo DB using Vector search.

Now in the same code folder, create a file to initialize the google llm model using google API. and this model should generate the answer based on the prompt and context provided by the Mongo DB i.e. retrieve function.

create an Langgraph connection where with the Graph like, Start-> User gives the query->go to llm model-> llm model decided whether to call the Mongo DB retrive tool or not -> after calling the tool, prompt+context should go back to the llm model-> check if again or any other tool needs to be called-> if not the llm should generate the reponse -> end. basically the graph should look like the below image.

Prepare me a word document which describe the flow /pipelines and architecture of this RAG project.  Don't make any changes in the code with this prompt, just prepare me a downloadable word document.

Instead of just using the MessagesState in Stategraph function create in MessagesState class and customize the structure that is necessary for this project. Also add the memory to the bot using checkpointers so that the bot can able to remember that conversation of that particular thread and able to answer with respect to the conversation chain.

Now I'm gonna define some of the important tools for this AI model. Rename and change the existing tool i.e. "retrieve_procurement_context" name to "Supplier Contract", which retrieves the basically supplier contract details from the Mongo DB. 

The second tools should be names as "Purchase Order" which is used to lookup the Purchase order tables in MySQL Database and retrieve the Purchase order details from the MySQL database. create an connection using Langchain/ LangGraph to MySQL database and database context should be provided to llm model with one row on the each table including headers in order to generate the SQL script per the user's questions. once the user ask question in natural language, LLM should covert the question into the SQL query and retrieve data from MySQL database and answer the user in natural language.

The second tools should be names as "Invoice" which is used to lookup the invoice tables in MySQL Database and retrieve the invoice details from the MySQL database. create an connection using Langchain/ LangGraph to MySQL database and database context should be provided to llm model with one row on the each table including headers in order to generate the SQL script per the user's questions. once the user ask question in natural language, LLM should covert the question into the SQL query and retrieve data from MySQL database and answer the user in natural language.

<Streamlit UI prompt>
Add a new Streamlit frontend to my existing procurement chatbot. Don't make changes to any of the file or existing code just create an new app.py file to create the Streamlit frontend.
First inspect my project and find the existing `ask(question, thread_id)` function. **Do not modify or rewrite my existing LangGraph, LLM, tools, database logic, checkpointer**, terminal `main.py` or any of the file.** Reuse the existing `ask()` function.

### UI
Create a new `app.py` with:
* Iron Man / JARVIS-inspired futuristic theme
* Dark background with subtle cyan/blue futuristic elements
* Clean HUD-style appearance
* ChatGPT-like conversation layout
* User and assistant messages displayed as chat messages
* `st.chat_input()` at the bottom for questions
* Clean, modern, uncluttered design
* Title this application as "ZAF"

### Thread ID
The user should NOT enter a thread ID.
Generate one automatically using Streamlit session state:
```python
if "thread_id" not in st.session_state:
    st.session_state.thread_id = str(uuid.uuid4())
```
Reuse the same thread ID for every question in that Streamlit session.
When the user enters a question:
```python
answer = ask(
    question,
    st.session_state.thread_id
)
```
Maintain the displayed chat history using `st.session_state`.
